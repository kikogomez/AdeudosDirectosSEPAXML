- Probar la librería de 'parsing' de CommandLineParser OK
- Configurarla para nuestras caracteristicas Ok
	-> -i RutaCompletaBaseDeDatos OK
	-> -o NombreFicheroXMLdeSalida Ok
- Configurar los 'Exit Codes' Ok
- Comprobar la conexion a la base de datos Ok
	-> Ver que los path funcionan Ok
	-> Ver que la cadena de conexion es valida Ok
- ¡HAY QUE MODIFICAR LA TABLA SEPAXMLDATOSENVIO!
	-> En CreditorID debería ir el NIF (por lo pronto lo extraemos del actual CreditorID, que es el AT02 completo)
	-> Igualmente debemos añadir el CreditorName (por lo pronto dejamos "NOMBRE ACREEDOR DE PRUEBAS")
	-> Añadir el CreditorAgentName, que no lo tenemos, y lo necesitamos para el DirectDebitRemmitance
	-> El BankCode no lo tenemos en la base de datos, pero lo podemos sacar del CreditorBankAccount
- ¿QUE PASA CON EL DEBTOR AGENT BIC? ¿De donde se toma al crear el nodo <DrctDbtTxInf>? No se incuye en DirectDebitTransaction...
	-> Posiblemnte se toma de los datos del DirectDebitRemmitance, por lo que no es necesario incluir el campo en la base de datos
- Cambiar el tipo 'double' por decimal en la base de datos?
- Comprobar la lectura de las tablas
	-> Devolver error si la tabla no existe (crear base de datos in la tabla)
	-> Devolver error si la tabla esta vacia (crear base de datos con tablas vacias)
- Asegurarnos de que no necesitamos tomar datos extras
	-> En PaymentIntructionInformationID, ¿como lo generamos?
		* En SEPAXMLRecibosTemporalSolorte no especificamos en que PaymentInstruction va cada uno. El TransactionID es secuencial sobre el MessageID
		* Lo generamos a partir del MessageID y le añadimos "-01" para las RCUR y "-02" para las FIRST. (o RC y FI)
